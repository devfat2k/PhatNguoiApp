import { StyleSheet } from 'react-native';
import { Padding } from '@src/utils/styles/spacing';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    paddingHorizontal: Padding._XLARGE,
  },
});
